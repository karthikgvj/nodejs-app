// Create a '.EnvLocal' file in the root directory to provide env variables.

PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_INSTANCE=
TRANSPORTER_EMAIL=
TRANSPORTER_PASSWORD=

// Run 'npm run local' to start the server

